type: object
title: Payment Detail information
description: the details of a done payment
required:
 - payment_method
 - amount
properties:
  payment_method:
    $ref: '#/definitions/PaymentMethod'
  amount:
    type: number
    format: float
    description: The amount that was payed.
