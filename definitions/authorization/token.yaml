type: object
title: Authentication information
required:
 - access_token
 - token_type
 - refresh_token
 - expires
properties:
  access_token:
    type: string
  token_type:
    type: string
  refresh_token:
    type: string
  expires:
    type: string
    format: date
