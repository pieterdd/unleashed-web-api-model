type: object
title: Invoice settings
required:
  - billing_preference
  - invoicing_address
properties:
  billing_preference:
    type: string
    enum:
      - paper
      - digital
  invoicing_address:
    $ref: '#/definitions/Address'
