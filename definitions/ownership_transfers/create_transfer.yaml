type: object
title: Create ownership transfer process
required:
  - transfer_to
  - subscription_id
properties:
  transfer_to:
    type: string
    format: email
    description: The email adress of the person to whom you want to transfer the ownership (it must be a customer).
  subscription_id:
    type: string
    description: The id of the subscription of which you want to transfer the ownership